import { theme } from "@/styles/theme";

export const TAG_COLOR_TYPE = {
  deck: `${theme.palette.gray_800}`,
  tile: `${theme.palette.gray_70}`,
};

export const TAG_TEXT_COLOR_TYPE = {
  deck: `${theme.palette.gray_0}`,
  tile: `${theme.palette.gray_800}`,
};
